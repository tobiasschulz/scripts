#!/bin/bash

. /etc/environment

cd /var/spool/hosts/

tinc-graph tinc.png

(
	cat /etc/motd.local
	echo "TINCPNG"
	echo
	sensors | sed 's@  ALARM@@gm'
) | aha --title $(hostname) \
	| sed 's@TINCPNG@<img src="tinc.png" />@gm' \
	> index.html

